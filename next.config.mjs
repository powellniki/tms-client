/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost', '192.168.1.204'], // Add your backend's domain here
    },
  };
  
  export default nextConfig;